const extension = {
    name: "myName", // string
    api: { f() {} }, // object of functions
    init ({app}) {}, // virtual
    start () {}, // virtual


    // api :
    // reserved :
    // start(), init(), on(), off(), once(), emit(), _eventCallbacks, app, ...

    // test() {}
}